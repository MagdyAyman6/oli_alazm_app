import 'package:flutter/material.dart';

class SebhaTabViewBody extends StatelessWidget {
  const SebhaTabViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("سبحه"));
  }
}
